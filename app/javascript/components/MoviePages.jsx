import * as React from "react";
import * as ReactDOM from "react-dom";

const MoviePages = () => {
  return <> Movie Page </>;
};
document.addEventListener("DOMContentLoaded", () => {
  ReactDOM.render(<MoviePages />, document.getElementById("movieReact"));
});
export default MoviePages;
