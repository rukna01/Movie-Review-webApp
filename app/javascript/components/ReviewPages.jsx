import * as React from "react";
import * as ReactDOM from "react-dom";

const ReviewPages = () => {
  return <>Review page</>;
};
document.addEventListener("DOMContentLoaded", () => {
  ReactDOM.render(<ReviewPages />, document.getElementById("reviewPages"));
});
export default ReviewPages;
